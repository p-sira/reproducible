[package]
name = "reproducible-macros"
version = "0.1.0"
edition = "2024"
description = "Procedural macros for the `reproducible` crate"
license = "BSD-3-Clause"

[lib]
proc-macro = true

[dependencies]
proc-macro2 = "1.0"
quote = "1.0"
syn = { version = "2.0", features = ["full"] }
